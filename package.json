{
  "name": "ompusage",
  "version": "1.0.0",
  "description": "OMP terminal widget for local AI token usage, provider quotas, reset windows, and balances.",
  "private": true,
  "scripts": {
    "test": "bun test ./index.test.ts",
    "build": "bun build ./index.ts --target=bun --outfile=./dist/index.js"
  },
  "omp": {
    "extensions": ["./index.ts"]
  }
}
